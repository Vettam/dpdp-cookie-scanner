---
"dpdp-cookie-scanner": patch
---

Register `dpdp-cookie-scanner` as a bin alias of `dpdp-cookie-scan` so `npx dpdp-cookie-scanner` can find an executable, and document `--package` in the READMEs.
