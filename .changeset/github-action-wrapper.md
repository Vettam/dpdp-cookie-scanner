---
"dpdp-cookie-scanner": minor
---

Add the GitHub Action wrapper (`action/`) that runs `dpdp-cookie-scan --ci`, uploads findings.json/md as an artifact, and posts or updates findings.md as a pull-request comment. Interpretation stays in the CLI.
