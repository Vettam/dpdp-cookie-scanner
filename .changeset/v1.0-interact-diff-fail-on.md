---
"dpdp-cookie-scanner": minor
---

Spec v1.0: three-pass banner interaction (`--interact`), ScanResult interpretation diff (`--diff-from` / `--diff-to`), and opt-in `--fail-on settled` that never trips on arguable or open findings.
